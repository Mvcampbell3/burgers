<div class="burger">
    <h2>{{name}}</h2>

    <button class="devourBtn btn" data-which="{{this.id}}" data-newDevour="{{devour}}">
        {{#if devoured}}Put Back{{else}}Devour{{/if}}
    </button>

    <button class="deleteBtn btn" data-which="{{this.id}}">
        Delete
    </button>
</div>